# 🚀 ХУТКІ СТАРТ

## 1️⃣ Усталёўка

```bash
# Усталёўка залежнасцей
pip install -r requirements.txt
```

### Калі патрэбны GUI

```bash
# Ubuntu/Debian
sudo apt-get install python3-pyqt5
pip install PyQt5

# Fedora
sudo dnf install python3-qt5
pip install PyQt5

# macOS/Windows
pip install PyQt5
```

## 2️⃣ Запуск

### Кансольная версія

```bash
./run.sh run
```

### Графічная версія

```bash
./run.sh gui
```

## 3️⃣ Тэставанне

```bash
# Аўтаматычныя тэсты
./run.sh test

# Праверка кніг
./run.sh books

# Тэст LLM (калі ёсць Ollama)
./run.sh llm-on ollama
./run.sh llm-test
```

## 4️⃣ Прыклады запытаў

```
Хто такі Кастусь Каліноўскі?
Што такое Статут 1588 года?
Вайна 1812 года ў Беларусі
Грунвальдская бітва 1410 года
Што такое БНР?
```

---

**Гатова!** 🎉
