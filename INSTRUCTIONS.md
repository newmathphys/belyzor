# 📚 ІНСТРУКЦЫЯ ПА ВЫКАРЫСТАННЮ КРЫНІЦ — БелУзор v0.1

## 🎯 Агляд крыніц

Сістэма **БелУзор v0.1** выкарыстоўвае некалькі тыпаў крыніц:

### 1. 📖 Падручнікі гісторыі (`data/raw/history/`)
- 8 школьных падручнікаў (6-11 класы)
- **Памер:** 3.78 MB
- **Фактаў:** ~27,000
- **Як выкарыстоўваць:** Аўтаматычна загружаныя пры ініцыялізацыі

### 2. 📚 Беларуская энцыклапедыя (`data/raw/bel-en/`)
- 18 тамоў энцыклапедыі
- **Памер:** ~203 MB
- **Фактаў:** ~72,000 (дададзены)
- **Статус:** ✅ Дададзена ў базу

### 3. 📖 Слоўнікі (`data/raw/dictionaries/`)
- 26 слоўнікавых баз
- **Тыпы:** тлумачальныя, перакладныя, арфаграфічныя
- **Выкарыстанне:** для лематызацыі і пошуку

---

## 🔧 Як дадаць кнігу/энцыклапедыю

### Спосаб 1: Праз вэб-інтэрфейс

1. Адкрыйце `http://localhost:5000`
2. У бакавой панэлі націсніце **"📁 Дадаць кнігу"**
3. Абярыце `.txt` файл
4. Абярыце рэжым:
   - **Па сэнтенцыях** — для дакладнага пошуку
   - **Па раздзелах** — для захавання кантэксту

### Спосаб 2: Праз кансоль

```bash
# Дадаць адну кнігу
./run.sh books-add data/raw/bel-en/encyclopedia_volume_01.txt

# Пераіндэксаваць усё
./run.sh books-reindex
```

### Спосаб 3: Праз Python

```python
from src.core.ultimate_engine_v1 import BelarusUltimateEngine

engine = BelarusUltimateEngine()

with open('data/raw/bel-en/encyclopedia_volume_01.txt', 'r', encoding='utf-8') as f:
    book_text = f.read()

# Дадаванне кнігі
count = engine.add_book(book_text, 'encyclopedia_vol_01', 'sentences')
print(f'Дададзена {count} фактаў')
```

---

## 📊 Статыстыка крыніц

```bash
# Праверыць статыстыку
./run.sh books-list

# Праверыць колькасць фактаў
python3 -c "
import json
with open('data/etalons/facts.json') as f:
    facts = json.load(f)
print(f'Усяго фактаў: {len(facts):,}')
"
```

---

## 🔍 Як сістэма шукае

### Этапы пошуку:

1. **Лематызацыя** — прывядзенне слоў да нармальнай формы
   - Выкарыстоўвае слоўнікі з `data/raw/dictionaries/`
   - Прыклад: "гісторыі" → "гісторыя"

2. **Пошук па індэксе** — TF-IDF падобнае ранжыраванне
   - Шукае па 347,517 індэксаваных словах

3. **Пашырэнне асацыяцыямі** — дадаванне сінонімаў
   - 59 асацыяцыйных сувязяў

4. **LLM генерацыя** (калі ўключана) — стварэнне звязнага адказу
   - З улікам кантэксту дыялогу

---

## 📍 Як паказваюцца крыніцы

### У вэб-інтэрфейсе:

Кожны адказ мае блок **"📚 Крыніцы"**:

```
📚 Крыніцы (5):
  [1] Кастусь Каліноўскі нарадзіўся ў 1838 годзе
      🔍 Google
  [2] Ён быў кіраўніком паўстання 1863 года
      🔍 Google
```

### Крыніцы з энцыклапедыі:

Калі факт з энцыклапедыі, ён будзе мець пазнаку:
```
[1] [tom_5_clean] Гісторыя Беларусі ў 9 тамах...
```

### Крыніцы з падручнікаў:
```
[1] [1] Падручнік гісторыі Беларусі для 8 класа...
```

---

## 🧹 Ачыстка энцыклапедыі ад дрэннага тэксту

### Праблемы энцыклапедый:
- Разрывы радкоў
- Лішнія прабелы
- Спецсимволы
- Неструктураваны тэкст

### Скрыпт ачысткі:

```python
import re

def clean_encyclopedia_text(text):
    """Ачыстка тэксту энцыклапедыі"""
    
    # Выдаленне лішніх прабелаў
    text = re.sub(r'\s+', ' ', text)
    
    # Выдаленне спецыяльных сімвалаў
    text = re.sub(r'[^\w\s.,;:!?()"\-–—\'а-яёўіА-ЯЁЎІ0-9]', '', text)
    
    # Нармалізацыя кропак і прагалаў
    text = re.sub(r'\.\s*\.', '.', text)
    text = re.sub(r'\s*([.,;:!?])\s*', r'\1 ', text)
    
    # Разбіццё на сказы
    sentences = re.split(r'([.!?]+\s*)', text)
    
    # Фільтрацыя кароткіх і пустых сказаў
    cleaned = []
    for s in sentences:
        s = s.strip()
        if len(s) > 20 and not s.isdigit():
            cleaned.append(s)
    
    return ' '.join(cleaned)

# Выкарыстанне
with open('encyclopedia_volume_01.txt', 'r', encoding='utf-8') as f:
    raw_text = f.read()

clean_text = clean_encyclopedia_text(raw_text)

with open('encyclopedia_volume_01_clean.txt', 'w', encoding='utf-8') as f:
    f.write(clean_text)
```

---

## 💾 Захаванне дыялогаў

### У вэб-інтэрфейсе:

1. **Аўтаматычнае захаванне** — кожны дыялог захоўваецца ў `localStorage`
2. **Кнопка "💾 Захаваць"** — ручное захаванне
3. **Кнопка "📂 Загрузіць"** — выбар захаванага дыялогу
4. **Кнопка "📁 Экспарт"** — экспарт у `.txt` файл

### Дэталёвая інфармацыя:

```javascript
// Захаванне ў localStorage
localStorage.setItem('belUzor_chats', JSON.stringify(chats));

// Структура дыялогу
{
  id: 1234567890,
  timestamp: "2026-02-17T13:30:00",
  messages: [
    {role: "user", content: "Хто такі Каліноўскі?"},
    {role: "assistant", content: "Кастусь Каліноўскі...", facts: [...]}
  ]
}
```

---

## 🗑️ Выдаленне старых дыялогаў

### У вэб-інтэрфейсе:

1. Націсніце **"📂 Загрузіць"**
2. Абярыце дыялог для выдалення
3. Націсніце **"🗑️"** побач з дыялогам

### Праз кансоль браўзера:

```javascript
// Выдаліць усе дыялогі
localStorage.removeItem('belUzor_chats');

// Прагледзець спіс
console.log(JSON.parse(localStorage.getItem('belUzor_chats')));
```

---

## 🧪 Тэставанне сістэмы

### 1. Запуск аўтатэстаў

```bash
./run.sh test
```

### 2. Праверка пошуку

```bash
# Кансольны тэст
echo "Хто такі Кастусь Каліноўскі?" | python3 src/core/ultimate_engine_v1.py

# Вэб-тэст
curl -X POST http://localhost:5000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "Хто такі Кастусь Каліноўскі?", "use_llm": true}'
```

### 3. Праверка крыніц

```python
import json

with open('data/etalons/facts.json', 'r', encoding='utf-8') as f:
    facts = json.load(f)

# Праверка крыніц
from collections import Counter
sources = Counter(fact.get('book', 'unknown') for fact in facts)

print("Крыніцы:")
for source, count in sources.most_common():
    print(f"  {source}: {count:,}")
```

---

## 📋 Чэк-ліст перад выкарыстаннем

- [ ] Устаноўлены ўсе залежнасці (`pip install -r requirements.txt`)
- [ ] Запушчаны вэб-сервер (`./run.sh web`)
- [ ] LLM уключаны (калі патрэбны разгорнутыя адказы)
- [ ] Энцыклапедыі дададзены ў базу
- [ ] Індэксацыя завершана (347,517 слоў)
- [ ] Браўзер адкрыты на `http://localhost:5000`

---

## 🎯 Прыклады выкарыстання

### Пытанне па энцыклапедыі:

```
❓ Што такое Люблінская унія?
→ Сістэма знойдзе факты з энцыклапедыі (том 5, 11)
→ LLM дасць разгорнуты адказ
→ Пакажа крыніцы з нумарамі тамоў
```

### Пытанне па падручніку:

```
❓ Калі адбылася Грунвальдская бітва?
→ Сістэма знойдзе ў падручніку гісторыі 6 класа
→ Дакладная дата: 15 ліпеня 1410 года
→ Крыніца: [2] Падручнік гісторыі 6 класа
```

### Дыялог з кантэкстам:

```
❓ Хто такі Скарына?
→ Адказ пра Францыска Скарыну

❓ Калі ён нарадзіўся?
→ Сістэма памятае кантэкст (Скарына)
→ Адказ: каля 1490 года

❓ Дзе ён вучыўся?
→ Сістэма ўсё яшчэ памятае кантэкст
→ Адказ: Кракаўскі ўніверсітэт, 1506 год
```

---

## 📞 Кантакты і дапамога

Калі ўзніклі пытанні:

1. Праверце лагі: `tail -f /tmp/web.log`
2. Праверце статыстыку: `curl http://localhost:5000/api/stats`
3. Перазапусціце сервер: `./run.sh web`

---

**БелУзор v0.1 — Працуем па ўзоры давераных кніг** 🇧🇾
