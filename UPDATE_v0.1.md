# 🧠🇧🇾 БЕЛУЗОР v0.1 — АПОШНІЯ ЗМЕНЫ

## 📋 Што дададзена

### 1. 📚 Раздзел "Матэрыялы"

Тры асобныя катэгорыі з магчымасцю хаваць/паказваць:

#### 📖 Энцыклапедыя (18 томаў)
- Асобны спіс усіх 18 томаў беларускай энцыклапедыі
- Паказаны памер кожнага тома
- Хуткі доступ да любой кнігі

#### 📕 Кнігі (падручнікі)
- 8 школьных падручнікаў па гісторыі
- Памер і назва
- Магчымасць дадаць новую кнігу

#### 📗 Слоўнікі
- 26 слоўнікавых баз
- Магчымасць прагляду зместу (націсніце на слоўнік)
- Паказваюцца першыя 50 запісаў для прадзагляду

### 2. 💬 Дыялогі (бакавая панэль)

#### Аўтаматычнае захаванне
- Пры пачатку новага дыялогу стары **аўтаматычна захоўваецца**
- Усе дыялогі адлюстроўваюцца ў бакавой панэлі злева
- Паказваецца:
  - Першае паведамленне (40 сімвалаў)
  - Дата і час
  - Колькасць паведамленняў

#### Кіраванне дыялогамі
- **Націсніце** на дыялог → загрузіць
- **🗑️** → выдаліць дыялог
- **🗑️ Выдаліць усе** → ачысціць усе дыялогі

#### Лічыльнік
- Паказвае агульную колькасць дыялогаў: `(5)`

---

## 🎨 Дызайн

### Бакавая панэль цяпер уключае:

```
┌─────────────────────────────┐
│ 🧠🇧🇾 БелУзор v0.1          │
│ Пошук па ўзоры давераных   │
├─────────────────────────────┤
│ ➕ Новы дыялог             │
├─────────────────────────────┤
│ 📊 Статыстыка              │
│ • Фактаў: 100,484          │
│ • Слоў: 347,517            │
│ • Кніг: 8                  │
├─────────────────────────────┤
│ 🤙 LLM Статус              │
│ [🟢] lmstudio / gemma-3    │
│ [Уключыць] [Адключыць]     │
├─────────────────────────────┤
│ ⚙️ Налады                  │
│ ☑ Выкарыстоўваць LLM       │
│ ☑ Паказваць крыніцы        │
├─────────────────────────────┤
│ 📚 Матэрыялы               │
│ ▼ Энцыклапедыя (18 томаў)  │
│   📖 Том 1 (5.5 MB)        │
│   📖 Том 2 (4.8 MB)        │
│   ...                      │
│ ▶ Кнігі (падручнікі)       │
│ ▶ Слоўнікі                 │
│ [📁 Дадаць кнігу]          │
├─────────────────────────────┤
│ 💬 Дыялогі (5)             │
│ Хто такі Каліноўскі?       │
│   17 лют. 13:45  12 пав. 🗑️│
│ Што такое Статут 1588?     │
│   17 лют. 13:30  8 пав. 🗑️ │
│ ...                        │
├─────────────────────────────┤
│ 2026 © БелУзор v0.1        │
└─────────────────────────────┘
```

---

## 🔧 Тэхнічныя дэталі

### Новыя API эндпоінты:

```javascript
GET /api/encyclopedias
// Вяртае спіс усіх 18 томаў энцыклапедыі

GET /api/dictionaries
// Вяртае спіс усіх 26 слоўнікаў

GET /api/dictionaries/<назва>
// Вяртае змест слоўніка (першыя 50 запісаў)
```

### Новыя функцыі JavaScript:

```javascript
loadEncyclopedias()     // Загрузка энцыклапедый
loadDictionaries()      // Загрузка слоўнікаў
viewDictionary(name)    // Прагляд слоўніка
toggleMaterials(type)   // Схаваць/паказаць раздзел
loadChatsList()         // Загрузка спісу дыялогаў
```

### Аўтаматычнае захаванне:

```javascript
function newChat() {
    // Аўтаматычнае захаванне бягучага дыялогу
    if (currentChatId || document.querySelectorAll('.message').length > 0) {
        saveChat();
    }
    
    // Стварэнне новага дыялогу
    document.getElementById('messages-container').innerHTML = '';
    conversationHistory = [];
    currentChatId = null;
    
    // Абнаўленне спісу дыялогаў
    loadChatsList();
}
```

---

## 📊 Структура захоўвання дыялогаў

```javascript
localStorage.setItem('belUzor_chats', JSON.stringify(chats));

// Структура:
[
  {
    id: 1708174500000,
    timestamp: "2026-02-17T13:45:00",
    messages: [
      {role: "user", content: "Хто такі Каліноўскі?"},
      {role: "assistant", content: "Кастусь Каліноўскі...", facts: [...]}
    ]
  },
  ...
]
```

---

## 🎯 Як карыстацца

### Прагляд энцыклапедый:

1. Адкрыйце бакавую панэль
2. Націсніце на **"▼ Энцыклапедыя (18 томаў)"**
3. Убачыце спіс усіх 18 томаў
4. Кожны том паказвае памер у MB

### Прагляд слоўнікаў:

1. Націсніце на **"▼ Слоўнікі"**
2. Націсніце на назву слоўніка
3. Адкрыецца модальнае акно з зместам
4. Паказваюцца першыя 50 запісаў

### Кіраванне дыялогамі:

**Пачаць новы дыялог:**
1. Націсніце **"➕ Новы дыялог"**
2. Стары дыялог аўтаматычна захаваны
3. Адкрыйце новы дыялог

**Загрузіць стары дыялог:**
1. Знайдзіце дыялог у бакавой панэлі
2. Націсніце на яго
3. Дыялог загружаны

**Выдаліць дыялог:**
1. Навядзіце на дыялог у бакавой панэлі
2. Націсніце **🗑️**
3. Пацвердзіце выдаленне

---

## ✅ Праверка

```bash
# Запуск вэб-сервера
./run.sh web

# Праверка API
curl http://localhost:5000/api/encyclopedias
curl http://localhost:5000/api/dictionaries

# Адкрыць у браўзеры
http://localhost:5000
```

---

## 📁 Файлы

### Абнаўлёныя файлы:

- `src/web/templates/index.html` — дададзены раздзелы матэрыялаў і дыялогаў
- `src/web/static/js/app.js` — новыя функцыі загрузкі
- `src/web/static/css/style.css` — стылі для новых элементаў
- `src/web/web_server.py` — новыя API эндпоінты

### Новыя файлы:

- `INSTRUCTIONS.md` — інструкцыі па крыніцах
- `USAGE.md` — поўнае кіраўніцтва
- `check_system.sh` — аўтаматычная праверка сістэмы

---

## 🎨 Візуальныя элементы

### Значкі матэрыялаў:

- 📖 Энцыклапедыя — сіні
- 📕 Кнігі — чырвоны
- 📗 Слоўнікі — зялёны

### Індыкатары:

- ▼ Раздзел адкрыты
- ▶ Раздзел закрыты

### Колеры:

- Энцыклапедыя: `#3498db` (сіні)
- Падручнік: `#27ae60` (зялёны)
- Выдаліць: `#e74c3c` (чырвоны)

---

**БелУзор v0.1 — Поўны кантроль над матэрыяламі і дыялогамі!** 🇧🇾
